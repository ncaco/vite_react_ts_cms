import React from 'react';
import { RouterProvider } from '@app/providers';

const App: React.FC = () => {
  return (
    <div className="min-h-screen">
      <RouterProvider />
    </div>
  );
};

export default App; 